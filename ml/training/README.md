# Training

`fit_demo.py` fits an Isolation Forest on the synthetic CDR feature vectors and
writes a local artifact to `ml/models/`.

Run it from the repository root:

```powershell
python ml/training/fit_demo.py
```

This is a reproducible synthetic baseline, not a proprietary trained model.