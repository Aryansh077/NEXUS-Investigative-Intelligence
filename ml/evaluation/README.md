# Evaluation

`run_demo.py` reports entity and relationship coverage plus the number of
runtime anomaly candidates. The synthetic repository has no labeled ground
truth, so accuracy, precision, recall, and F1 are intentionally not reported.

```powershell
python ml/evaluation/run_demo.py
```