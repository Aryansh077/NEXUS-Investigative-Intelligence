# Models

Generated model artifacts belong here. They are local build outputs and are not
committed by default. The API fits a deterministic Isolation Forest at request
time; `ml/training/fit_demo.py` is available when a local serialized baseline is
needed for experimentation.