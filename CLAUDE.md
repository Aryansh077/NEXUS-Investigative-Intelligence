# NEXUS Project Context

The repository is a local FastAPI and React prototype. SQLite and NetworkX are
the default local persistence and graph layers. The anomaly baseline is an
Isolation Forest fitted at runtime on synthetic case features. It is not a
pretrained proprietary model and has no labeled accuracy claim.